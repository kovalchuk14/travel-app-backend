description: Internal Server Error
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: Something went wrong
        data:
          type: object
          properties:
            message:
              type: string
              example: Internal Server Error
